# تطبيق آل سعيدان - منصة إدارة العائلة الشاملة

## نظرة عامة
تطبيق متكامل لإدارة شؤون عائلة آل سعيدان، يوفر نظاماً شاملاً لإدارة أفراد العائلة، تنظيم الفعاليات، إرسال الدعوات الذكية، وإدارة مكتبة التجارب والخبرات.

## المميزات الرئيسية

### 🌳 شجرة العائلة التفاعلية
- عرض شامل لجميع أفراد العائلة مرتبة حسب الأجيال
- معلومات تفصيلية لكل فرد (الاتصال، التخصص، الإنجازات)
- نظام إدارة متكامل للأعضاء (إضافة، تعديل، حذف)
- تصنيف حسب مستوى القرابة (مباشرة، قريبة، بعيدة)

### 🎉 نظام الفعاليات والدعوات الذكية
- إنشاء وإدارة فعاليات العائلة المختلفة
- نظام دعوات ذكي حسب مستوى القرابة والأجيال
- تتبع استجابات الدعوات (مقبولة، مرفوضة، ربما)
- إحصائيات مفصلة لكل فعالية
- رسائل مخصصة مع الدعوات

### 💡 نظام المقترحات "شاركنا أفكارك"
- تلقي مقترحات من أفراد العائلة
- تصنيف المقترحات حسب النوع والأولوية
- تتبع حالة كل مقترح ومراجعته

### 📚 مكتبة التجارب والخبرات
- مجموعة شاملة من تجارب وخبرات أفراد العائلة
- تصنيف المحتوى (مقالات، فيديوهات، صوتيات، وثائق)
- فلترة حسب الفئة (الأعمال، التعليم، التطوير الشخصي، إلخ)
- نظام المحتوى المميز والأكثر مشاهدة

## التقنيات المستخدمة

### Backend
- **Hono Framework** - إطار عمل خفيف وسريع للـ edge computing
- **Cloudflare Workers** - منصة serverless للتشغيل على الشبكة الحافية
- **Cloudflare D1** - قاعدة بيانات SQLite موزعة عالمياً
- **TypeScript** - لبرمجة آمنة ومُنظمة

### Frontend
- **HTML/CSS/JavaScript** - تقنيات الويب الأساسية
- **Tailwind CSS** - إطار عمل CSS للتصميم السريع
- **Font Awesome** - مكتبة الأيقونات
- **Axios** - مكتبة HTTP client للتعامل مع API
- **Noto Sans Arabic** - خط عربي احترافي

### Infrastructure
- **Cloudflare Pages** - استضافة المواقع الثابتة
- **PM2** - مدير العمليات للتطوير المحلي
- **Vite** - أداة البناء والتطوير

## هيكل المشروع

```
webapp/
├── src/
│   ├── index.tsx          # تطبيق Hono الرئيسي
│   └── renderer.tsx       # قالب HTML الأساسي
├── public/
│   └── static/
│       ├── app.js         # JavaScript للواجهة الأمامية
│       └── style.css      # تنسيقات CSS مخصصة
├── migrations/
│   └── 0001_initial_schema.sql  # هيكل قاعدة البيانات
├── seed.sql               # بيانات تجريبية للعائلة
├── wrangler.jsonc         # إعدادات Cloudflare
├── ecosystem.config.cjs   # إعدادات PM2
└── package.json           # إعدادات المشروع والتبعيات
```

## قاعدة البيانات

### الجداول الرئيسية
- **family_members** - معلومات أفراد العائلة
- **events** - فعاليات العائلة
- **event_invitations** - دعوات الفعاليات وحالاتها
- **suggestions** - مقترحات الأعضاء
- **library_content** - محتوى مكتبة التجارب
- **family_council** - أعضاء مجلس الأسرة
- **awards** - نظام الجوائز والتقدير

## API Endpoints

### إدارة أفراد العائلة
- `GET /api/family-members` - جلب جميع أفراد العائلة
- `POST /api/family-members` - إضافة عضو جديد
- `GET /api/family-members/:id` - جلب بيانات عضو محدد
- `PUT /api/family-members/:id` - تحديث بيانات عضو
- `DELETE /api/family-members/:id` - إلغاء تفعيل عضو

### إدارة الفعاليات والدعوات
- `GET /api/events` - جلب جميع الفعاليات
- `POST /api/events` - إنشاء فعالية جديدة
- `POST /api/events/:id/send-invitations` - إرسال دعوات للفعالية
- `GET /api/events/:id/invitations` - جلب دعوات فعالية معينة
- `GET /api/events/:id/invitation-stats` - إحصائيات الدعوات
- `POST /api/invitations/:id/respond` - الرد على دعوة

### المقترحات
- `GET /api/suggestions` - جلب جميع المقترحات
- `POST /api/suggestions` - إضافة مقترح جديد

### مكتبة التجارب
- `GET /api/library` - جلب محتوى المكتبة
- `GET /api/library/featured` - جلب المحتوى المميز
- `GET /api/library/categories` - جلب فئات المحتوى
- `POST /api/library/view/:id` - تسجيل مشاهدة

## التطوير المحلي

### المتطلبات
- Node.js 18+
- npm أو yarn
- Wrangler CLI

### التشغيل
```bash
# تثبيت التبعيات
npm install

# تطبيق هجرات قاعدة البيانات
npm run db:migrate:local

# تحميل البيانات التجريبية
npm run db:seed

# بناء المشروع
npm run build

# تشغيل التطوير المحلي
npm run dev:sandbox
```

### الأوامر المفيدة
```bash
# إعادة تعيين قاعدة البيانات
npm run db:reset

# فحص قاعدة البيانات
npm run db:console:local

# تشغيل مع PM2
pm2 start ecosystem.config.cjs
```

## النشر

### Cloudflare Pages
```bash
# إعداد API مفتاح Cloudflare
wrangler auth login

# إنشاء مشروع Pages
wrangler pages project create al-saedan-family-app --production-branch main

# نشر التطبيق
npm run deploy
```

## الميزات المتقدمة

### نظام الدعوات الذكية
- استهداف حسب مستوى القرابة (صغير، متوسط، كبير)
- استهداف حسب الأجيال
- اختيار أعضاء محددين
- رسائل مخصصة
- تتبع شامل للاستجابات

### حماية البيانات
- إلغاء تفعيل بدلاً من الحذف النهائي
- حماية من حذف الأعضاء الذين لهم أطفال
- تشفير البيانات في النقل والتخزين

### تحسينات الأداء
- تحميل تفاعلي للبيانات
- ذاكرة تخزين مؤقت ذكية
- تحسين الاستعلامات
- ضغط الأصول

## البيانات النموذجية

### هيكل العائلة المحمل
- **الجيل الأول**: الشيخ محمد بن عبدالله بن سعيدان (المؤسس)
- **الجيل الثاني**: عبدالله، فهد، حمد، إبراهيم محمد آل سعيدان
- **الجيل الثالث**: سلمان عبدالله (رئيس المجلس)، خالد فهد، هشام حمد، بدر إبراهيم آل سعيدان

### مجلس الأسرة الحالي
- **الرئيس**: سلمان عبدالله آل سعيدان
- **الأعضاء**: خالد فهد، هشام حمد، بدر إبراهيم آل سعيدان

## معلومات التواصل
- **للدعم التقني**: info@salmansaedan.com
- **الهاتف**: 0533361154
- **الموقع**: [سيتم إضافة URL الإنتاج]

## الترخيص
هذا المشروع خاص بعائلة آل سعيدان. جميع الحقوق محفوظة.

---
تم تطوير هذا التطبيق باستخدام أحدث التقنيات لضمان الأداء والأمان والقابلية للتوسع.